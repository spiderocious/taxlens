# Refuse any package published less than 7 days ago — cheapest defence against
# fresh supply-chain attacks. Don't lower without an explicit reason + follow-up.
minimum-release-age=10080

# Fail install if Node/pnpm versions don't satisfy package.json engines.
engine-strict=true
